spring:
  profiles:
    active: local

---
spring:
  config:
    activate:
      on-profile: local

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 269542961934-l0t5sg2o7ikk20c3bkgudfsevqs36uvi.apps.googleusercontent.com
            client-secret: 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
            scope:
              - profile
              - email
  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:testdb
      driver-class-name: org.h2.Driver
      username: sa
      password:
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
        implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy

jwt:
  secret: token-secret-key
  tokenExpiration: 600000 # 10분 (1000 * 60 * 10)
  refreshTokenExpiration: 1209600000 # 14일 (1000 * 60 * 60 * 24 * 14)